<%= simple_form_for(@team) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :nome %>
  </div>
	<%= f.association :members, label_method: :email, value_method: :id, include_blank: false%>
  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
